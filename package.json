{
  "name": "moodle-homework-planner",
  "version": "1.0.0",
  "description": "Moodle Homework Planner - Full Stack Application",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "dev": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "moodle",
    "homework",
    "planner",
    "education",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "multer": "^2.0.2",
    "recharts": "^3.2.1",
    "tesseract.js": "^6.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
